## Airflow Local State

This directory is for local Airflow experimentation only.

- Do not commit `airflow.cfg`, `airflow.db`, logs, or generated state.
- Configure Airflow through local environment variables or a local untracked `airflow.cfg`.
- Keep any machine-specific paths and secrets out of the repository.
