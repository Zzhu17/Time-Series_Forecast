# Terraform

This is a minimal IaC skeleton to help you wire up a cloud environment later.

Start with `terraform/aws` (AWS provider). You will need an AWS account and credentials
configured to run `terraform init/plan/apply`.
